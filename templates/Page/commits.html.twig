{% extends 'base.html.twig' %}
{% block title %}Inicio - AppSoga{% endblock %}
{% block body %}
    {% include('menu.html.twig') %}
    <div class="container-fluid">
        <div class="container">
            <div class="row">
                <h1 style="text-align: center;">Actualizaciones</h1><br>
                <div class="col-md-12" style="text-align: justify;">
                    <p>Nuestro software se actualiza cada instante con el fin de mantener los módulos en pleno
                        funcionamiento y con la menor cantidad de errores posibles.</p>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="page">
                        <div class="page__demo">
                            <div class="main-container page__container">
                                <div class="timeline">
                                    <div class="timeline__group">
                                        <span class="timeline__year">Actualizaciones recientes</span>
                                        {% if commits is defined and commits|length > 0 %}
                                            {% for commit in commits %}
                                                <div class="timeline__box">
                                                    <div class="timeline__date">
                                                        <span class="timeline__day">{{ commit.commit.author.date|date('d') }}</span>
                                                        <span class="timeline__month">{{ commit.commit.author.date|date('M') }}</span>
                                                    </div>
                                                    <div class="timeline__post">
                                                        <div class="timeline__content">
                                                            <img src="{{ commit.committer.avatar_url }}" alt="WA"
                                                                 class="img-responsive"
                                                                 style="border-radius: 50%;width: 40px;"/>
                                                            <h3>{{ commit.commit.author.name }}</h3>
                                                            <i style="color: #444444;">{{ commit.sha }}</i><br>
                                                            <p> {{ commit.commit.message }}</p>
                                                        </div>
                                                    </div>
                                                </div>
                                            {% endfor %}
                                        {% else %}
                                            <h5>Sin Actualizaciones</h5>
                                        {% endif %}
                                        </ul>
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>
    {% include('pie.html.twig') %}
{% endblock %}